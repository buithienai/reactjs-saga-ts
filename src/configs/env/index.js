const app = {
    env: {
        localhost: 'localhost',
        production: 'production',
        development: 'development',
        staging: 'staging',
    },
};

export default Object.assign(app);
